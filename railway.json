{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --frozen-lockfile=false"
  },
  "deploy": {
    "startCommand": "pnpm --filter syrus-api start",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300
  }
}
